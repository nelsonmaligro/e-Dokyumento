#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../pdf2json/bin/pdf2json" "$@"
  ret=$?
else 
  node  "$basedir/../pdf2json/bin/pdf2json" "$@"
  ret=$?
fi
exit $ret
